#
# ~/.config/termite/config
#

[options]
resize_grip = false
scroll_on_output = false
audible_bell = true
mouse_autohide = true

# Make any URL clickable
clickable_url = true

# Font
font = Inconsolata 11

# Cursor blinking: "system", "on" or "off"
cursor_blink = system

# Cursor shape: "block", "underline" or "i-beam"
cursor_shape = underline

[colors]
foreground = rgba(255, 255, 255, 1)
background = rgba(0, 0, 0, 0.85)
